{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/fixes/(.*)", "dest": "/src/fixes/$1" },
    { "src": "/(.*)", "dest": "/src/index.js" }
  ]
}

# Self-signed certificate generation command (for initial setup before certbot)
# Run this on the server:
#
# openssl req -x509 -nodes -days 3650 -newkey rsa:2048 \
#   -keyout nginx/ssl/key.pem \
#   -out nginx/ssl/cert.pem \
#   -subj "/C=CN/ST=State/L=City/O=Relay/CN=localhost"
